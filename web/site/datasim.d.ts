// Type stub for /static/datasim.js: the in-browser Datastar
// simulator, served separately and kept external by the build. A
// rooted import specifier can't be covered by an ambient `declare
// module`, so this is wired in via `paths` in tsconfig.json.
// biome-ignore lint/suspicious/noExplicitAny: type stub for the external, untyped datasim.js simulator.
declare const sim: any;
export default sim;
