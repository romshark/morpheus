# Dependencies
node_modules/

# Generated static site (rebuilt by `make gen`)
dst/

# Compiled gen binary if `go build ./internal/cmd/gen` is run at the root
# (gen is normally invoked via `go run`, not built to disk).
/gen

# Build outputs from web/build.mjs (esbuild) land in /min/ at the repo
# root and are checked in — that directory is the vendorable artifact
# set for downstream users and the source the static-site gen copies
# into dst/static/min/.

# macOS
.DS_Store

# Editor / IDE
.idea/
*.swp
*~
